<local:ChannelSuggestionViewBase
    x:Class="ChatModApp.Views.ChannelSuggestionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChatModApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <PersonPicture Grid.Column="0" Name="Thumbnail" HorizontalAlignment="Left" VerticalAlignment="Stretch" />
        <TextBlock Grid.Column="1" Name="Channel" 
                   VerticalAlignment="Center" HorizontalAlignment="Center"
                   FontWeight="Bold" FontSize="20" />
        <TextBlock Grid.Column="2" Name="StreamStatus" 
                   Text="Live" 
                   VerticalAlignment="Center" HorizontalAlignment="Right"
                   Foreground="Red" FontSize="20" FontWeight="SemiBold" Margin="10,0" />
    </Grid>
</local:ChannelSuggestionViewBase>